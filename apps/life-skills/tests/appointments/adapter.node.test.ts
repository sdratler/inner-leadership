import test from 'node:test'; import assert from 'node:assert/strict'; import {AcademyCalendarAdapter,ACADEMY_CALENDAR_ID,getBookingEligibility,createManualReview} from '../../src/lib/providers/google-calendar/adapter.ts';
const facts={consentCleared:true,paymentVerified:true,payAtSessionApproved:false};
test('eligibility',()=>{assert.equal(getBookingEligibility({...facts,consentCleared:false}).eligible,false);assert.equal(getBookingEligibility({consentCleared:true,paymentVerified:false,payAtSessionApproved:true}).paymentDue,true);});
test('manual bridge',()=>{const m=createManualReview('booking_url_unverified',true);assert.equal(m.state,'manual_review');assert.equal(m.calendarId,ACADEMY_CALENDAR_ID);});
test('manual reservation and reconciliation',async()=>{const a=new AcademyCalendarAdapter({listEvents:async()=>[]});const r={requestId:'r',entitlementId:'e',participantId:'p',type:'child' as const,start:'2026-09-20T10:00:00+03:00',idempotencyKey:'k'};const m=await a.reserveAppointment(r,facts);assert.equal(m.status,'pending');assert.equal(a.applyCalendarEventChange({idempotencyKey:'k',providerEvent:{id:'evt',start:r.start,end:'2026-09-20T11:00:00+03:00',status:'confirmed'}}).status,'confirmed');});
test('conflict and cancellation',async()=>{const a=new AcademyCalendarAdapter({listEvents:async()=>[{id:'busy',start:'2026-09-20T10:10:00Z',end:'2026-09-20T11:30:00Z'}]});await assert.rejects(()=>a.reserveAppointment({requestId:'r',entitlementId:'e',participantId:'p',type:'parent_call',start:'2026-09-20T10:00:00Z',idempotencyKey:'x'},facts),/conflict/);const b=new AcademyCalendarAdapter({listEvents:async()=>[]});await b.reserveAppointment({requestId:'r2',entitlementId:'e',participantId:'p',type:'parent_call',start:'2026-09-20T10:00:00Z',idempotencyKey:'z'},facts);assert.equal(b.recordCancellationNotice('r2','2026-09-19T01:00:00Z').status,'pending');});
test('occurrence and participants',async()=>{const a=new AcademyCalendarAdapter({listEvents:async()=>[]});const m=await a.reserveAppointment({requestId:'r3',entitlementId:'e',participantId:'a',type:'parent_call',start:'2026-09-20T10:00:00Z',idempotencyKey:'q'},facts);a.applyCalendarEventChange({idempotencyKey:'q',providerEvent:{id:'evt',recurringEventId:'series',start:m.time.start,end:m.time.end,status:'confirmed'}});assert.equal(a.setParticipant('r3','b',false,true).participants.length,1);});
