import {notFound} from "next/navigation";import {isLocale} from "../../../../lib/locale.ts";import {ClientsRoster} from "../../../../features/cases/clients-roster.tsx";export const dynamic="force-dynamic";export default async function Page({params,searchParams}:{params:Promise<{locale:string}>;searchParams:Promise<{section?:string;filter?:string}>}){const {locale}=await params;if(!isLocale(locale))notFound();const query=await searchParams;return <ClientsRoster locale={locale} section={query.section} prospectFilter={query.filter}/>;}
