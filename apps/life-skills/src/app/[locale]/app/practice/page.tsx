import {notFound} from "next/navigation";import {isLocale} from "@/lib/locale.ts";import {Ls040FeaturePage} from "@/features/home-practice/feature-page.tsx";export default async function Page({params,searchParams}:{params:Promise<{locale:string}>;searchParams:Promise<Record<string,string|string[]|undefined>>}){const {locale}=await params;if(!isLocale(locale))notFound();const q=await searchParams;return <Ls040FeaturePage locale={locale} role="practitioner" kind="home-practice" caseId={typeof q.caseId==="string"?q.caseId:undefined} audienceId={typeof q.audienceId==="string"?q.audienceId:undefined} assignmentId={typeof q.assignmentId==="string"?q.assignmentId:undefined}/>}
