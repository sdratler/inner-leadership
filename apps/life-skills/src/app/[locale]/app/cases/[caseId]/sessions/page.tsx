import {notFound} from "next/navigation";import {isLocale} from "@/lib/locale.ts";import {SessionListWorkspace} from "@/features/session-workflow/workspace.tsx";export const dynamic="force-dynamic";export default async function Page({params}:{params:Promise<{locale:string;caseId:string}>}){const {locale,caseId}=await params;if(!isLocale(locale)||!/^[0-9a-f-]{36}$/i.test(caseId))notFound();return <SessionListWorkspace locale={locale} caseId={caseId}/>;}
