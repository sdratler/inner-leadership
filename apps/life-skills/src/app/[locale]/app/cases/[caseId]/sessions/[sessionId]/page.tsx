import {notFound} from "next/navigation";import {isLocale} from "@/lib/locale.ts";import {SessionDetailWorkspace} from "@/features/session-workflow/workspace.tsx";export const dynamic="force-dynamic";export default async function Page({params}:{params:Promise<{locale:string;caseId:string;sessionId:string}>}){const {locale,caseId,sessionId}=await params;if(!isLocale(locale)||![caseId,sessionId].every(value=>/^[0-9a-f-]{36}$/i.test(value)))notFound();return <SessionDetailWorkspace locale={locale} sessionId={sessionId}/>;}
