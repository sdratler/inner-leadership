import {notFound} from "next/navigation";import {isLocale} from "@/lib/locale.ts";import {UpdatesWorkspace} from "@/features/updates/updates-workspace.tsx";export default async function Page({params,searchParams}:{params:Promise<{locale:string}>;searchParams:Promise<Record<string,string|string[]|undefined>>}){const {locale}=await params;if(!isLocale(locale))notFound();const q=await searchParams;return <UpdatesWorkspace locale={locale} role="parent" initialCaseId={typeof q.caseId==="string"?q.caseId:undefined} initialAudienceId={typeof q.audienceId==="string"?q.audienceId:undefined} initialPracticeVersionId={typeof q.practiceVersionId==="string"?q.practiceVersionId:undefined}/>}
