import {notFound} from "next/navigation";import {isLocale} from "@/lib/locale.ts";import {Ls040FeaturePage} from "@/features/home-practice/feature-page.tsx";export default async function Page({params,searchParams}:{params:Promise<{locale:string}>;searchParams:Promise<Record<string,string|string[]|undefined>>}){const {locale}=await params;if(!isLocale(locale))notFound();const query=await searchParams;return <Ls040FeaturePage locale={locale} role="adult_client" kind="home-practice" caseId={typeof query.caseId==="string"?query.caseId:undefined} audienceId={typeof query.audienceId==="string"?query.audienceId:undefined} assignmentId={typeof query.assignmentId==="string"?query.assignmentId:undefined}/>}
