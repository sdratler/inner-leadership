import {notFound} from "next/navigation";import {isLocale} from "@/lib/locale.ts";import {UpdatesWorkspace} from "@/features/updates/updates-workspace.tsx";export default async function Page({params,searchParams}:{params:Promise<{locale:string}>;searchParams:Promise<Record<string,string|string[]|undefined>>}){const {locale}=await params;if(!isLocale(locale))notFound();const query=await searchParams;return <UpdatesWorkspace locale={locale} role="adult_client" initialCaseId={typeof query.caseId==="string"?query.caseId:undefined}/>}
