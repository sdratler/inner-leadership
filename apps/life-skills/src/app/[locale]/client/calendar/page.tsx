import {notFound} from "next/navigation";import {CalendarWorkspace} from "@/features/calendar/workspace.tsx";import {calendarPageSession} from "@/features/calendar/page-session.ts";import {civilDate,shiftDay} from "@/features/calendar/time.ts";import {isLocale} from "@/lib/locale.ts";export const dynamic="force-dynamic";export default async function Page({params,searchParams}:{params:Promise<{locale:string}>;searchParams:Promise<Record<string,string|string[]|undefined>>}){const {locale}=await params;if(!isLocale(locale))notFound();const session=await calendarPageSession(["adult_client","child"]);const query=await searchParams;let date=civilDate(new Date().toISOString());try{if(typeof query.date==="string")date=shiftDay(query.date,0)}catch{notFound()}const view=query.view==="day"||query.view==="month"?query.view:"week",caseId=typeof query.caseId==="string"&&/^[0-9a-f-]{36}$/i.test(query.caseId)?query.caseId:"";return <CalendarWorkspace locale={locale} role={session.role} initialDate={date} initialView={view} initialCaseId={caseId}/>}
