# Creative reconciliation map — 11 September 2026

Patch the existing lanes against the accepted contract commit. Do not install old packets and then append another contradictory instruction.

| Existing source/lane | Required disposition |
| --- | --- |
| PR #16 `creative/manifests/**` | Reconcile the same manifest paths/asset IDs with this inventory. Preserve verified asset provenance. Remove the generic compositor as visual author; no competing manifest directory. |
| PR #16 `creative/lib/render.mjs` | Historical renderer, not the master author. Any retained raster editor is restricted to explicit masks/clean underlays and tested preservation outside the edit. |
| PR #22 `config/life-skills-brand-kit.v1.json` and `src/state.mjs` | Replace tool-local brand truth with a pointer/loader for the accepted `creative/**` records; no fallback on missing/stale manifests. |
| PR #22 intent instructions/compiler/assertions/tests | Remove UI-free/live-headline/live-benefits rule. Website executor emits exact master reference plus header/WhatsApp placement only. Tests must reject visible reconstruction and falsely approved/missing artifacts. |
| PR #22 plan/execute | Local zero-spend plan for structured jobs; separately meter optional LLM parsing. Implement a real OpenArt adapter, bounded queue, idempotent jobs, credit cap and durable receipts. A delegated prompt/stdout is not a completed bulk engine. |
| PR #23 website hero | Remove copy/icons/photo reconstruction; use the four promoted rasters. Keep visible live header/button and equivalent hidden semantics. Verify file bytes and four actual viewport/locale proofs before claiming completion. |
| Drive Website Brief §21–22, MKT-060 earlier layering paragraphs | Historical superseded instructions. Current Website §23 and MKT-060 final locked-master section override. Never concatenate both into an execution prompt. |
| Drive ad-production handoff v4.9 | Replace old read-first versions and fixed style-only reference rule with current source pointers and promotion-aware ratio-master resolution. Retire explicitly rejected C03/C05 copy from active jobs. |
| Historical campaign ZIPs, old website handoffs, this chat's V4 `HERO-MASTER.json` | Provenance only. A layout spec, font/hex table or old 'live HTML text' directive is not an owner-approved raster. Stop feeding them to current jobs. Retain originals/source photos and valid approval evidence. |
| OpenArt uploaded `plate-4x5-openart.png` and four blank-plate outputs | Existing work, not final hero masters. Do not promote or use as composition authority merely because it is uploaded or completed. |

Do not merge stale #16/#22/#23 wholesale. Preserve active worker ownership; the audit does not claim their work or authorize release. Source `sdratler/inner-leadership` and downstream deployment `shloimie-beep/bnei-neviim-academy` are separate. Once the scoped reconciliation is accepted, record its exact commit in the existing handoff/Build Control; subsequent workers use that pointer, not 'latest file with master in its name'.
