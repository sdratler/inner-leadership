# Continue Life Skills: master images, website release, 30 creatives and operating workflow

This is the consolidated execution handoff requested by the owner in the audit conversation. It supersedes earlier handoffs that told Codex to reconstruct the approved artwork. It is an instruction to complete the work, not evidence that the work has run.

## Ground the current state

Read AGENTS.md, the registered CURRENT sources, `docs/PROJECT-WORKFLOW.md`, its audit, `creative/README.md`, the creative audit and manifests. Re-read exact main/PR heads, current work ownership and downstream release state. The audit baseline was `0af6981cbab1c5e79ebc0ffb4d29bd08020b7729`; do not assume it is still current.

The audit reconciled the native project-settings blocks under `AUDIT-CREATIVE-SOT-20260911-01`. The owner then requested a shorter paste: `OWNER-INSTRUCTIONS-SHORT-20260911-01` updates Operating Protocol 2.7 and Prompt Pack 2.9 with an identical block of 6,748 characters (783 words, including delimiters and final newline). The local paste file matches that shared block; the preparation, claim and privacy clauses remain verbatim. Older source receipts do not cover this change. Re-admit affected work through its existing independently verified source-checker procedure using the actual approved diff and current revisions; never use blind accept-current or weaken checks. Preserve unaffected work. A canonical settings readback does not mean the owner's Project Settings UI was updated.

Source repo: `sdratler/inner-leadership`. Public deployment source is downstream `shloimie-beep/bnei-neviim-academy`. Do not modify the BNA academy homepage as part of a Life Skills hero task. Preserve unrelated worker changes and active claims. Use isolated branches/worktrees, the current lane-specific coordinator and draft PRs; do not merge stale PR16/22/23 wholesale or weaken source/CI checks.

Resolve private provider/source bindings through the existing MKT-060 Codex Prompt Link, section `private_asset_bindings`. Those source IDs and checksums are already known: no new upload request to the owner is needed for the chosen reference or registered photos. Fetch originals through authorized access and verify actual bytes before a paid edit. Do not publish private bindings or photographs in this public repository.

## Lane 1 — finish the image masters first

Use a real image-generation/editing capability through the existing OpenArt connection. Codex can call an image model; it must not replace that call with HTML/CSS/SVG artwork or a screenshot. Retrieve the current model schema and quote exact settings. Use the approved reference, not a generated blank plate or an older forest/beige flyer. The authoritative visual is the chosen centered teal image.

First produce exactly **one EN-mobile candidate**. Remove the toolbar/header and painted WhatsApp control from the selected reference. Change the first English icon label to the owner's newly confirmed **Self-governance**. Apply subsequent correction `OWNER-HERO-TYPE-20260911-01`: make the English age line slightly bolder toward the supplied Hebrew reference weight, preserving its size, tan color and position. Keep all remaining photo/text/icon/shading/fade/cream/curve pixels and geometry intact. Prepare a precise permitted edit mask/underlay. If the provider cannot enforce a local edit, do not promise pixel preservation from prose; use a supported controlled editing route and compare outside the edit, or report the exact capability limitation. Do not default to full-frame redraw or premium batch generation.

Capability checked in this audit: the exposed OpenArt MCP image-editing forms provide references and prompts, but no protected-region mask or exact patch operation. The connector also has a user-driven upload picker, not a direct local-file import endpoint. The source reference is already uploaded. For exact local corrections, establish a controlled patch workflow and a verified import/storage route available to the executor; do not buy a more expensive model or ask the owner to reupload the same reference to conceal this gap.

Inspect the actual result before presenting it. Check identity, spelling, text spacing, no face overlap, no duplicated logo, no bottom branches and no damaged fade. Show the candidate with an actual live-toolbar/live-button preview. One owner review promotes that exact file; prior choices are not reopened. Then complete HE-mobile, EN-desktop and HE-desktop one at a time, with matching centered art direction and independently verified text. Desktop is not a side layout or stretched mobile image.

Final hero image contains headline, service, age, real photo, three benefit icons/labels, teal atmosphere, cream fade and lower curve. Only the toolbar/header and working WhatsApp control are excluded. Keep the Hebrew headline `בכל ילד יש גיבור`, service `טיפול רגשי לבנים`, age `בגילאי 8–12`, and registered benefit labels. Preserve the approved Hebrew brand spelling/tagline. No founder name/signature in the hero.

Match the live WhatsApp pill to `copy.json` visualControl: rounded dark teal body, brighter upper rim, soft shadow, cream WhatsApp glyph/divider/bold localized label/right arrow. Approximate mobile reference bounds are x80/y1144/w782/h125 on941×1672; remeasure on the final toolbar-free master before integration. The desktop attachment is a chat screenshot, not a deployable master.

Save each actual image to the existing project/durable asset destination, register actual dimensions/SHA256/locale/orientation and approval tied to that hash, then measure the reserved WhatsApp bounds. Do not invent final hashes, mark missing assets approved, or claim four masters from a manifest alone. Reuse logical IDs from `website-hero-masters.json`.

## Lane 2 — integrate and release the website

Reconcile PR23 against the promoted masters. Render exact approved image bytes with responsive source selection and proportional display scaling, without CSS recoloring/filtering/gradients, silent crop or copy/icon reconstruction. Prevent automatic image re-encoding. Reserve intrinsic aspect ratio before load. Place live controls in registered bounds; matching semantic copy may be visually hidden. Validate the actual deployed file bytes as well as DOM behavior.

Carry forward the owner's existing website refinements, checking the current deployed version before doing duplicate work:

- Hebrew/English desktop/mobile, correct direction, current small header logo with tight brand/tagline spacing, a simple mobile contact/sign-up action, and clear working floating WhatsApp contact.
- Localized WhatsApp editable prefills and short labels from the registered copy/destination. A website click opens a draft; it never sends automatically.
- Requested section order: hero; outcomes with the child-and-rabbit photo used once and zoomed out; approved LB social proof; integrative approach and twelve meetings. Preserve accepted content and resolve any newer source conflict before changing the order.
- Mobile twelve-meeting section is one horizontally swipable, full-panel carousel; vertical scrolling remains free. Each panel has its title and concise modality/practice bullets. No forced tour through twelve stacked pages, clipped text or faces covered by copy. Desktop retains comfortable spacing and clear hierarchy.
- Approach copy is concise, grounded and specific: intrinsic motivation/self-governance, values and choices, learning from mistakes/natural consequences, goal setting, IFS/parts terminology, CBT, mindfulness/meditation and body awareness as approved. Remove the rejected flow filler and robot-sounding phrasing. No invented dietary protocol, credentials or fixed clinical outcome.
- Founder/children-on-grass photo is the whole About-section setting with readable biography, preserving faces; avoid the rejected split photo card and decorative artifacts.
- Existing owner-approved LB testimonial/portrait attribution uses `LB, 2024`; consent was explicitly confirmed in the conversation. Preserve exact approved text and context; do not turn the quote into a general medication claim or ad promise.
- Small linked BNA mark/project footer, orderly phone/contact/terms/privacy. BNA fundraiser conversion remains its own work item; do not let it block Life Skills.

Run meaningful build, link, RTL and control checks and inspect actual screenshots at representative mobile and desktop widths in both languages. Ensure no horizontal overflow or delayed layout jump. Do not report screenshots inspected if no browser ran. Do not invent an app/login completion percentage from source files.

The owner has explicitly requested the Life Skills website live. After the exact release candidate and target are verified and the applicable merge/deployment gates are satisfied, carry out the authorized release and return its live URL, source SHA, downstream commit/artifact digest and provider deployment receipt. Preserve rollback. If one real deployment capability is missing, finish all preparation and report that exact blocker; do not ask again whether the owner wants the site live. This instruction does not authorize bypassing protected checks or unrelated provider changes.

## Lane 3 — 30 actual ad creatives

The required deliverable is **30 finished creatives**, with real founder-only and founder-with-son photo variations, Hebrew and English, consistent centered teal styling. It is not 30 prompts or a new set of independently invented designs. Use the registered 30-job draft queue to make the count explicit; finalize the draft copy after the hero review. Do not revive rejected C03/C05 text. Do not promise clinical results after twelve sessions.

Establish approved 4:5 feed and 9:16 vertical compositions before batching. Use accepted ratio/locale masters and exact photo/copy bindings. Generate genuine photo/composition changes only through the selected image model after a fidelity proof. Exact copy/logo/icon edits require controlled patches/layers and preservation checks. No automatic model substitution or expensive fallback.

Prepare a quote for the complete queue with actual settings and expected output count. Apply an existing applicable image-credit cap; if none exists, return one batch estimate and ask for that one financial limit before submission. Do not confuse the old Meta campaign budget with image credits. Continue all zero-cost preparation while that is unresolved.

Bulk submission depends on the minimum Lane 4 adapter, queue, recovery and credit controls actually working. Watchdog installation does not block the first hero proof. Use one claimed job per distinct request, save the exact provider history ID and resume it across interruptions. Download and verify output files, spelling/Hebrew, identity, dimensions and allowed drift. Return all 30 actual image links/files, a contact sheet and a machine-readable receipt table. Feed/vertical exports are derivatives of those designs; state actual counts separately. A failed or unapproved image does not count as finished.

Only after approved assets exist should the existing marketing scheduler consume their IDs. Recover the intended organic/status/group destinations, timezone, exclusions and daily cadence from current sources; do not invent WhatsApp groups, recipients, holiday dates or recurring budgets. Preserve the owner's no-holiday-ad instruction where applicable. Generation alone never proves a post was sent.

## Lane 4 — fix the operator and install the economical watchdog

Reconcile the useful PR16 registry work and PR22 SDK into one source system. Operator config holds resolvers/adapters, not a second brand kit. Implement the ramble workflow in `docs/PROJECT-WORKFLOW.md`: capture, distinguish facts/choices/proposals, probe only meaningful gaps, compile a precise diff, update existing canonical sources, invalidate dependent contracts, execute, verify and save receipts under one change ID.

Structured `plan` must be local and make no paid model/provider call. Free-form interpretation is optional and separately metered. Implement the missing OpenArt adapter, bounded queue, atomic job claims, deduplication, credit cap, saved outputs and crash recovery. Test ambiguous submission recovery: do not submit a duplicate after timeout. Schema/version mismatch blocks only dependent work. Photo B becomes available when its exact binding is registered, not by changing hardcoded logic.

Audit existing watchdog infrastructure before creating anything. Main at this audit had two CI workflows and no scheduled source collector; external scheduler state is unverified. Reuse the existing source-audit/Workflow Control architecture and required trusted boundaries. Do not modify expected hashes to manufacture PASS or give untrusted PR code provider credentials.

Install a real scheduled check in an authorized execution host, with ordinary code for version/hash changes, unresolved transactions, stale claims, missing assets/receipts and source/deployment divergence. Healthy runs use no LLM. AI is invoked only for a substantive exception that needs interpretation. Automatically repair reproducible derived indexes/projections and interrupted authorized bookkeeping; never merge owner intent, approve art, recolor masters, change budgets or weaken gates automatically.

Register the scheduler in the existing Releases/runtime ledger with job ID, timezone/cadence, source commit, effects and last result. Provide a real successful scheduled-event receipt in addition to a manual smoke test. Exercise synthetic changed-source, semantic-conflict, duplicate-job and expired-claim cases. Demonstrate a healthy run makes zero model calls, safe derived repairs are idempotent, and failed/missed runs produce a persisted visible exception. If only code was prepared, report `prepared`, not `watchdog installed`.

The no-publication flags in `creative/manifests/production-policy.json` limit effects of image-generation jobs; they are not a project-wide ban. Website release, approved posting and lead ingestion use their own scoped authorization and receipts. Do not enable publishing merely to generate an image.

## Funnel and final closeout

Preserve the existing lead-only WhatsApp response requirement: no chatbot on every private chat. Verify source attribution, one appropriate acknowledgment, human handoff and one idempotent row update in the existing Leads CRM spreadsheet. Do not put clinical details in the marketing sheet. Test through a designated authorized test path; no unsolicited real-contact messages. Wappy status publishing, Meta activation and lead ingestion retain distinct credentials, scopes and receipts.

Update the existing source records, active work and Change Log once per accepted change. Use the same project-instructions source; the owner can paste `docs/PROJECT-INSTRUCTIONS-TO-PASTE.md`, but do not claim it is installed until actual settings readback. Keep the parent app/BNA fundraiser/backlog visible in existing work records without inventing a parallel roadmap.

Return: corrected source/PR links and exact heads; actual four hero files/proofs; tested live website URL/release identity or precise blocker; actual30 creatives/contact sheet or exact remaining count; OpenArt credits quoted/spent/unknown; watchdog installation evidence; lead-to-CRM test result; and one next action. Do not end with a generic statement that everything is autonomous.
